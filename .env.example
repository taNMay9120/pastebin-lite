# Base URL for paste links (used in API responses)
# In production, this should be your deployed domain
NEXT_PUBLIC_BASE_URL=https://pastebin-lite-git-main-tanmay-s-projects-dcd0ebf5.vercel.app

# Test mode - set to "1" to enable deterministic time testing
# This allows the x-test-now-ms header to override current time
# TEST_MODE=1

# Database URL (when migrating to persistent storage)
# DATABASE_URL=postgresql://user:password@localhost:5432/pastebin

# Redis URL (for KV-store based persistence)
# REDIS_URL=redis://localhost:6379
